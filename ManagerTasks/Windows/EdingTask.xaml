<Window x:Class="ManagerTasks.Windows.EdingTask"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ManagerTasks.Windows"
        mc:Ignorable="d"
        Title="Edit Task" Height="400" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Название задачи -->
        <Label Content="Title:" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="TitleTextBox" Grid.Row="0" Grid.Column="1" Margin="5" Text="{Binding Title}"/>

        <!-- Описание задачи -->
        <Label Content="Description:" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="DescriptionTextBox" Grid.Row="1" Grid.Column="1" Margin="5" Text="{Binding Description}"/>

        <!-- Срок выполнения -->
        <Label Content="Due Date:" Grid.Row="2" Grid.Column="0" Margin="5"/>
        <DatePicker x:Name="DueDatePicker" Grid.Row="2" Grid.Column="1" Margin="5" SelectedDate="{Binding DueDate}"/>

        <!-- Статус задачи -->
        <Label Content="Status:" Grid.Row="3" Grid.Column="0" Margin="5"/>
        <ComboBox x:Name="StatusComboBox" Grid.Row="3" Grid.Column="1" Margin="5" 
                  DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding StatusId}"/>

        <!-- Назначенный пользователь -->
        <Label Content="Assigned User:" Grid.Row="4" Grid.Column="0" Margin="5"/>
        <ComboBox x:Name="AssignedUserComboBox" Grid.Row="4" Grid.Column="1" Margin="5" 
                  DisplayMemberPath="Username" SelectedValuePath="Id" SelectedValue="{Binding AssignedUserId}"/>

        <!-- Проект -->
        <Label Content="Project:" Grid.Row="5" Grid.Column="0" Margin="5"/>
        <ComboBox x:Name="ProjectComboBox" Grid.Row="5" Grid.Column="1" Margin="5" 
                  DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding ProjectId}"/>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" Grid.Row="7" Grid.Column="1" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Margin="5" Click="SaveButton_Click"/>
            <Button Content="Cancel" Width="80" Margin="5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
