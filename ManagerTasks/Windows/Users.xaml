<Window x:Class="ManagerTasks.Windows.Users"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ManagerTasks.Windows"
        mc:Ignorable="d"
        Title="Users" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Button Content="Редактировать" Margin="10" Click="EditUserButton_Click"/>
            <Button Content="Удалить" Margin="10" Click="DeleteUserButton_Click"/>
            <Button Content="Вернуться к задачам" Margin="10" Click="BackToTasksButton_Click"/>
        </StackPanel>
        <DataGrid x:Name="UsersGrid" AutoGenerateColumns="False" CanUserAddRows="False" Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role.Name}"/>
                <DataGridTextColumn Header="Team" Binding="{Binding Team.Name}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
